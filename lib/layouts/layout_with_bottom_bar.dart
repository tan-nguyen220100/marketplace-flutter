import 'package:flutter/material.dart';

class LayoutWithBottomBar extends StatelessWidget {
  const LayoutWithBottomBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
